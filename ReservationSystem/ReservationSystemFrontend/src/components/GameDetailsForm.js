import React from 'react'

export default function GameDetailsForm(props) {
    const {gameId} = props;
    return (
        <div>
            <p>Game Details form {gameId}</p>
        </div>
    )
}
